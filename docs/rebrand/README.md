# Rebrand landing page (draft)

Status: v1 proposal. Not linked from the live site. Does not replace `docs/index.html`.

Open before anything ships (art direction v2):

1. Color policy: glitch-artifact color only plus one terminal accent, or keep the current green/cyan/violet.
2. Portraits: AI-generated B&W humans (owned in a caption) or Ivan's own portrait.
3. Hero line: pick the "your agent knows you" variant.

Rules already decided:

- Emotional presentation first, technical content (loop, files, markers, install) after.
- Copy claims come from the README of the current release.
- No LLM-tell constructions in taglines (em-dash apposition, contrastive negation).

Open `index.html` directly in a browser. Fonts load from Google Fonts; everything else is inline.
