# Retrospective Distillation (synthetic fixture, files-only BRIDGE release)
AURA_FIXTURE_LINE: files-only
AURA_FIXTURE_PAYLOAD: dispatcher-1.2.0-bridge
Channel manifest: channels/manifest.json (read for the notice only; auto-update never fetches a software base)
Store: {DISTILL_DIR}
